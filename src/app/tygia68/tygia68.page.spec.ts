import { ComponentFixture, TestBed } from '@angular/core/testing';
import { Tygia68Page } from './tygia68.page';

describe('Tygia68Page', () => {
  let component: Tygia68Page;
  let fixture: ComponentFixture<Tygia68Page>;

  beforeEach(() => {
    fixture = TestBed.createComponent(Tygia68Page);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
